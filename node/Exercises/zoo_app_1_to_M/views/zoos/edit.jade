extends ../layout.jade

block content
  h1 Edit a new zoo

  form(action="/owners/#{zoo.owner_id}/zoos/#{zoo.id}?_method=PATCH" method="POST")
    include _form

  form(action="/owners/#{zoo.owner_id}/zoos/#{zoo.id}?_method=DELETE" method="POST")
    input(type="submit" value="X")
